<table>
  <tr>
    <td>Favorite</td>
    <td>Line</td>
    <td>Underdog</td>
  </tr>
  <% @games.each do |game| %>
    <tr>
      <td><%= game.favorite_display_name %></td>
      <td><%= text_field_tag(game.line) %> </td>
      <td><%= game.underdog_display_name %></td>
      <td><%= check_box_tag(game.line) %></td>
    </tr>
  <% end %>
</td>
  
